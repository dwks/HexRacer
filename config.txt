################
# Settings for the project

#map = models/testterrain.obj
map = models/testtrack.obj

########
### Display settings

# Setting width and height to 0 uses the current resolution
# Likewise, when bpp is 0 the current colour depth is used
display.width      = 800
display.height     = 600
display.fullscreen = 0
display.bpp        = 0

########
### Physics settings

# Acceleration and responsiveness of input
physics.constant.turn             = 0.15
physics.constant.accel            = 2.8
physics.constant.centripetal      = 1.0

# Vehicle suspension
physics.driving.enablesuspension  = 1
physics.driving.minlength         = 0.1
physics.driving.restlength        = 0.5
physics.driving.stretchlength     = 0.8
physics.driving.constant.k        = 1.0
physics.driving.constant.c        = 20.0

# Drag forces
physics.driving.lineardrag        = 0.4
physics.driving.angulardrag       = 0.5
physics.driving.sidewaysdrag      = 10.0

########
### Rendering settings

# Generate paint cells (takes some time)
render.paint.enabled = 1

# Testing: enable legacy painting code? (works but is slower)
render.paint.legacypaint = 0

# Rendering quality
#    0 = low
#    1 = medium
#    2 = high
render.quality = 2

########
### Network settings

# Network settings for both client and server
network.port = 1820
network.host = localhost
